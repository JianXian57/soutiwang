真实手机测试发现：导入 .xlsx 失败，Toast 显示：

导入失败：
org.dhatim.fastexcel.reader.DefaultXMLInputFactory

我提供的真实题库文件是标准 xlsx：
- Sheet1
- 第 1 行：题干、答案、选项A、选项B、选项C、选项D、选项E、选项F
- 共约 301 行
- 第 2 行开始是题目

请修复 xlsx 导入功能。

重要要求：

1. 不要继续依赖 fastexcel-reader
   fastexcel-reader 在 Android 真机上疑似存在 XMLInputFactory / StAX 兼容问题。

2. 请改成 Android 兼容的 xlsx 解析方案
   推荐自写轻量 xlsx 解析器：
   - 使用 ZipInputStream / ZipFile 读取 .xlsx
   - 解析 xl/sharedStrings.xml
   - 解析 xl/worksheets/sheet1.xml
   - 使用 Android 内置 XmlPullParser 或 SAX
   - 不依赖 javax.xml.stream.XMLInputFactory

3. 解析时必须支持：
   - shared string：`<c t="s"><v>index</v></c>`
   - inline string：`<c t="inlineStr">...`
   - 普通字符串/数字单元格
   - 空单元格
   - 根据单元格引用如 A1、B1、C1 还原列位置，不能因为空单元格导致列错位
   - 至少支持 A-F 选项列
   - 优先读取第一个工作表即可，后续再扩展多 Sheet

4. 导入预览修复
   这份文件导入后，预览页不应该显示“没有可预览的导入内容”。
   应该显示：
   - 总行数约 300
   - 可导入题目数约 300
   - 异常行数为 0 或很少
   - 前几条题目预览

5. 错误提示优化
   不要直接把类名显示给用户。
   例如不要显示：
   org.dhatim.fastexcel.reader.DefaultXMLInputFactory

   应改成：
   “导入失败：无法解析该 Excel 文件，请确认文件为 .xlsx 格式，且包含题干和答案列。”

   同时在 Logcat 中打印完整异常，方便开发调试。

6. 请把我这份真实题库文件加入测试验证
   文件名：2026年度岗位练兵（考试题库）.xlsx
   验证它能成功导入。

7. 修复后重新提交，并触发 GitHub Actions 构建 APK。
